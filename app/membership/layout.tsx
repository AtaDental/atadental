export const metadata = {
  title: "Membership Plan — $300/Year Dental Savings",
  description: "No insurance? Ata Dental's membership plan covers cleanings, exams, X-rays, and fluoride for $300/year, plus 20% off all other procedures.",
  alternates: { canonical: "/membership" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
