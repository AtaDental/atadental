export const metadata = {
  title: "Cherry Financing — 0% APR Payment Plans",
  description: "Get the dental care you need today with Cherry financing at Ata Dental. No hard credit checks, 0% APR options, and flexible monthly payments.",
  alternates: { canonical: "/financing" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
